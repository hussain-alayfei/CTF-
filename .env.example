# Meras CTF — frontend environment
# These are PUBLIC values (the anon key is protected by Row Level Security),
# so they are safe to expose in the browser. The app also ships with these
# same values baked in as a fallback, so setting them is optional.

VITE_SUPABASE_URL=https://xehzdlfrzlokwvtcfvjx.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_tvECgVRLC9NtXPwbgNOvDQ_PzKHmMuP
